# orden de pedido
Puede estar __pendiente__ de confirmacion hasta que el distribuidor la cambia a __aceptada__ o __rechazada__ dependiendo si puede o no surtir el pedido.

# pedido
cuando un distribuidor __acepta__ una orden de pedido esa orden se convierte en un __pedido__.